rootProject.name = 'event-publisher'

includeBuild '../../shared/common-objects'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../../gradle/libs.versions.toml"))
        }
    }
}
