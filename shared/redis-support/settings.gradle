rootProject.name = 'redis-support'

includeBuild '../../shared/common-objects'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../../gradle/libs.versions.toml"))
        }
    }
}
