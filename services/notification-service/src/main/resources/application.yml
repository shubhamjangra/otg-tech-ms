spring:
  config:
    name: notification-service
    import: common.properties
  profiles:
    active: local

---
spring:
  config:
    activate:
      on-profile: local
    import: application-local.properties